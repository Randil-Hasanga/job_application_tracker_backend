export interface User {
    _id: string;
    displayName: string;
    email: string;
    picture: string;
    createdAt: string; // or Date, depending on usage
    updatedAt: string; // or Date
  }
  